<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#ffffff"
    >

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:src="@drawable/subway_lady"
        android:scaleType="centerCrop"
        />

    <ImageView
        android:id="@+id/loginNameApp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/logo_white"
        android:layout_centerHorizontal="true"
        />

    <EditText
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:hint="Correo"
        android:layout_below="@+id/loginNameApp"
        android:id="@+id/loginCorreo"
        android:layout_marginTop="5sp"
        android:background="@drawable/login_edit_text"
        android:padding="8dp"
        android:textColor="#fff"
        android:textColorHint="#fff"
        android:layout_marginRight="10dp"
        android:layout_marginLeft="10dp"
        />

    <EditText
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/loginCorreo"
        android:hint="Contraseña"
        android:inputType="textPassword"
        android:layout_marginTop="5sp"
        android:padding="8dp"
        android:background="@drawable/login_edit_text"
        android:textColor="#fff"
        android:textColorHint="#fff"
        android:id="@+id/loginPswd"
        android:layout_marginRight="10dp"
        android:layout_marginLeft="10dp"
        />

    <Button
        android:layout_marginTop="5sp"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="Ingresar"
        android:id="@+id/loginButton"
        android:background="@drawable/login_button"
        android:textColor="#fff"
        android:layout_below="@+id/loginPswd"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_marginRight="10dp"
        android:layout_marginLeft="10dp"
        />




</RelativeLayout>
